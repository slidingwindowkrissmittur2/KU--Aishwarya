 int result = 0;
        foreach(int c in nums) {
            result ^= c;
        }
        return result;
!!!!
Array.Sort(A);

	if (A[0] != 1) {
	    return 0;
	}

	for (int i = 0; i < A.Length - 1; i++) {
	    if (A[i] + 1 != A[i + 1]) {
		return A[i]+1;
	    }
	}
	return 1;
    }
