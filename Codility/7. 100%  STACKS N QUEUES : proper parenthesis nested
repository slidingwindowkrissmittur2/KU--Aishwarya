int n =S.Length;
Stack<char> sta=new Stack<char>();
int i=0;
while(i<n){
if(S[i]=='(') 
  sta.Push(')');
else if(S[i]=='[')
  sta.Push(']');
else if(S[i]=='{') 
  sta.Push('}');
else if(S[i]==')') 
  sta.Pop();
else if(stack.Count==0|| sta.Pop()!=S[i])
  return 0;


i++;
}
return 1;
